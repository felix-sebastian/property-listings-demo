import React from "react";
import styled from "styled-components";

const Box = styled.div`
  flex-grow: 1;
  padding: 1rem;
  background-color: #777;
`;

export default () => <Box>hi from listings</Box>;
