import React from "react";
import styled from "styled-components";

const Box = styled.div`
  padding: 1rem;
  background-color: #555;
  color: #fff;
`;

export default () => <Box>hi from filters</Box>;
